import { MSAllowRoleDirective } from './allow-role.directive';

describe('AllowRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new MSAllowRoleDirective();
    expect(directive).toBeTruthy();
  });
});
